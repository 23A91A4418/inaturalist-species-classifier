version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: inaturalist-species-classifier:latest
    container_name: species_classifier_app
    volumes:
      - .:/app
    env_file:
      - .env
    stdin_open: true
    tty: true
    command: bash
